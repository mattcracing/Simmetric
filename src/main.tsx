import React from 'react'
import ReactDOM from 'react-dom/client'
import SimagicPedalTelemetry from './components/Home.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <SimagicPedalTelemetry />
    </React.StrictMode>,
)
